//Create a function that takes a string and adds space between each character as well as double the existing space.
function spacify(str) {
  //Use functions .split and .join to separate and then combine individual characters together with an additional space.
  return str.split('').join(' ');
}
